<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".components.host.HostRequestsFragment">

    <!-- Search Filter Bar -->
    <LinearLayout
        android:id="@+id/searchFilterBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@color/white"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Search Accommodation -->
        <LinearLayout
            android:id="@+id/searchAccommodationLayout"
            style="@style/SearchBarInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                style="@style/SearchBarIcon"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="Search Accommodation Icon"
                android:src="@drawable/ic_search" />

            <AutoCompleteTextView
                android:id="@+id/searchAccommodationEditText"
                style="@style/SearchBarText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:alpha="0.8"
                android:background="@android:color/transparent"
                android:hint="Search accommodation..." />
        </LinearLayout>

        <!-- Date Picker -->
        <LinearLayout
            android:id="@+id/datePickerLayout"
            style="@style/SearchBarInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                style="@style/SearchBarIcon"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="Date Picker Icon"
                android:src="@drawable/ic_calendar_black" />

            <EditText
                android:id="@+id/datePickerEditText"
                style="@style/SearchBarText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:alpha="0.8"
                android:background="@android:color/transparent"
                android:clickable="true"
                android:focusable="false"
                android:hint="Select Dates" />

        </LinearLayout>

        <LinearLayout
            android:id="@+id/ClearAndFilterLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="10dp"
            android:paddingEnd="10dp">

            <!-- Filter Button -->
            <TextView
                android:id="@+id/filterButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_gravity="start"
                android:text="Filter"
                android:textSize="16sp"
                android:textColor="#A9A9A9"
                android:background="@android:color/transparent"
                android:textStyle="bold"
                android:padding="8dp"
                android:textAllCaps="false"
                android:gravity="start"
                android:backgroundTint="@android:color/transparent"
                android:textAppearance="?attr/textAppearanceBody1"
                android:clickable="true"
                android:focusable="false" />

            <!-- Clear Button -->
            <TextView
                android:id="@+id/clearButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_gravity="end"
                android:text="Clear"
                android:textSize="16sp"
                android:textColor="#A9A9A9"
                android:background="@android:color/transparent"
                android:textStyle="bold"
                android:padding="8dp"
                android:textAllCaps="false"
                android:gravity="end"
                android:backgroundTint="@android:color/transparent"
                android:textAppearance="?attr/textAppearanceBody1"
                android:clickable="true"
                android:focusable="false" />

        </LinearLayout>


    </LinearLayout>

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="8dp"
        android:scrollbars="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/searchFilterBar"
        app:layout_constraintVertical_bias="0.0" />
</androidx.constraintlayout.widget.ConstraintLayout>
